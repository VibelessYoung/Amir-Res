<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    ScrollReveal().reveal(".tick", {
      duration: 1000,
      origin: "left",
      distance: "20px",
      easing: "ease-in-out",
      reset: false,
    });
  });
  document.addEventListener("DOMContentLoaded", () => {
    ScrollReveal().reveal(".MySwiper", {
      duration: 1000,
      origin: "right",
      distance: "20px",
      easing: "ease-in-out",
      reset: false,
    });
  });
</script>
